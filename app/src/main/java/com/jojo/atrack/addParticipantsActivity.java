package com.jojo.atrack;import androidx.appcompat.app.AppCompatActivity;import android.content.Intent;import android.database.Cursor;import android.os.Bundle;import android.text.Editable;import android.text.TextWatcher;import android.text.method.KeyListener;import android.view.KeyEvent;import android.view.View;import android.widget.EditText;import android.widget.TextView;import android.widget.Toast;public class addParticipantsActivity extends AppCompatActivity {    TextView tvTitle, tvName, tvPosition, tvPartIn1, tvPartIn2;    EditText etEmpID;    dbHelper DBh;    cls jcls = new cls();    String SeminarID;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_add_participants);        DBh = new dbHelper(this);        etEmpID = (EditText) findViewById(R.id.xpartID);        tvName = findViewById(R.id.xpartName);        tvPosition = findViewById(R.id.xpartPosition);        tvTitle = findViewById(R.id.xadpTitle);        tvPartIn1 = findViewById(R.id.xpartIn1);        tvPartIn2 = findViewById(R.id.xpartIn2);        Intent intent = getIntent();        tvTitle.setText(intent.getStringExtra("Title"));        SeminarID = intent.getStringExtra("SeminarID");        etEmpID.setOnKeyListener(new View.OnKeyListener() {            @Override            public boolean onKey(View v, int keyCode, KeyEvent event) {                String EmpID = etEmpID.getText().toString();                //when enter key was pressed                if (keyCode == KeyEvent.KEYCODE_ENTER && jcls.isNotEmpty(EmpID)) {                    Cursor res = DBh.selParticipant(EmpID);                    if(res.getCount() == 1){                        //record participant                        DBh.insParticipant(addParticipantsActivity.this, EmpID, SeminarID);                        //display participant info                        res.moveToNext();                        tvName.setText(res.getString(0));                        tvPosition.setText(res.getString(1));                        Cursor LogRecord = DBh.getLogRecord(addParticipantsActivity.this, SeminarID, EmpID);                        tvPartIn1.setText("In: " + LogRecord.getString(0) + ", Out: " + LogRecord.getString(1));                        tvPartIn2.setText("In: " + LogRecord.getString(2) + ", Out: " + LogRecord.getString(3));                        etEmpID.setText(null);                    } else                    {                        tvName.setText("UNKNOWN");                        tvPosition.setText("Position");                        Toast.makeText(addParticipantsActivity.this, "Invalid ID Number", Toast.LENGTH_LONG).show();                    }                }                return false;            }        });    }    @Override    public void onBackPressed()    {        Intent intent = new Intent(this,HomeActivity.class);        startActivity(intent);    }}